---
- name: "test de la solution drupal"
  hosts: all
  gather_facts: True
  tasks:
    - name: "début du test"
      debug:
        msg: "Voici le début du test"
    - name: "get homepage HTTP code"
      uri:
        url: http://www.test.tf/fake.html
